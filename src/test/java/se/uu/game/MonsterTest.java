import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

/**
 * Simulated test class to show how and what the class is supposed to test upon
 * implementation in future update of the game. // dessa simuleringar har kommenterats bort tillsvidare
 */
//class MonsterRoomTest {

   // @Test
   // void correctAnswerDamagesMonster() {
   //     Player player = new Player(30);      // start-HP
    //    Monster monster = new Monster(20);   // monster-HP
      //  MonsterRoom room = new MonsterRoom(monster, player);

      //  room.attack("2+3", "5");  // right answer

      //  assertTrue(monster.getHp() < 20,
                //"Monster takes damage with right answer");
      //  assertEquals(30, player.getHp(),
       //         "Player takes damage with wrong answer");
    //}

   // @Test
   // void wrongAnswerDamagesPlayer() {
    //    Player player = new Player(30);
     //   Monster monster = new Monster(20);
       // MonsterRoom room = new MonsterRoom(monster, player);

       // room.attack("2+3", "7");  // wrong answer

      //  assertTrue(player.getHp() < 30,
             //   "Player takes damage with wrong answer");
      //  assertEquals(20, monster.getHp(),
             //   "Monster does not takes damage with wrong answer");
   // }

  //  @Test
    //void monsterDefeatedMarksRoomCleared() {
      //  Player player = new Player(30);
        //Monster monster = new Monster(10);
        //MonsterRoom room = new MonsterRoom(monster, player);

        // Answer correctly until monster dies
        //while (!monster.isDead()) {
         //   room.attack("1+1", "2");
       // }

        //assertTrue(monster.isDead(),
            //    "Monstret has to be out of health");
      //  assertTrue(room.isCleared(),
             //   "Room is marked as cleared when monster is dead");
   // }

  //  @Test
    //void playerDiesIfTooManyWrongAnswers() {
      //  Player player = new Player(10);
        //Monster monster = new Monster(20);
        //MonsterRoom room = new MonsterRoom(monster, player);

        // Enough wrong answers kills the player
        //while (!player.isDead()) {
          //  room.attack("1+1", "99"); // always wrong
       // }

     //   assertTrue(player.isDead(),
              //  "Player should die with too many wrong answers");
     //   assertFalse(room.isCleared(),
              //  "Room is not cleared if player dies before defeating the monster ");
    //}
//}
